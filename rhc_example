import utils

rhc_grid = {
        "problem": [problem],
        "max_attempt": [50,500,1000,...],
        "max_iter": [...],
        "restart": [...],
    }
    rhc_best_param = utils.tune_params(rhc_grid, utils.rhc_run)
    rhc_results, rhc_wt = utils.rhc_run(**rhc_best_param)    
